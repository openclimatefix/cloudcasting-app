name: Build docker
on: push

jobs:
  DockerBuild:
    name: Build dockerr
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Build Docker image
        run: docker build .
